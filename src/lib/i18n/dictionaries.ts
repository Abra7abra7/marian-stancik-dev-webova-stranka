import sk from '@/locales/sk.json';
import en from '@/locales/en.json';
import pl from '@/locales/pl.json';

export type Language = 'sk' | 'en' | 'pl';

export const dictionaries = {
    sk,
    en,
    pl
};

